---
platform: linux

image_resource:
  type: docker-image
  source: {repository: starkandwayne/concourse
}

inputs:
- name: nginx-on-concourse
params:
  BOSH_CA_CERT: "((config.bosh.ca ))"
  BOSH_CLIENT: "((config.bosh.client ))"
  BOSH_CLIENT_SECRET: "((config.bosh.clientSecret ))"
  BOSH_ENVIRONMENT: "((config.bosh.environment ))"
  BOSH_ACTION: deploy
run: 
  path: nginx-on-concourse/Ressources/installationNginx.sh